Navigate to https://dev.mysql.com/downloads/windows/installer/5.7.html

install the MySQL Community Downloads, first choice
Choose Developer Default on first window
choose only defaults
create a MySQL Root Password, remember to save it.

Open MySQL Workbench

download mysql-connector-python and python-dotenv
currently using the root database. create a file called .env and type MY_PASSWORD="", inserting your root password into the quotes.